# React Vanilla JS

Este repositorio incluye el código usado en el módulo de React con Vanilla Javascript en ConquerBlocks.

Para correrlo:

1. Instala dependencias:

````
npm i
````

o con yarn:

````
yarn
````

2. Corre el proyecto en el servidor de desarrollo:

````
npm run dev
````

o con yarn

````
yarn dev
````